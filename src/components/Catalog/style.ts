import styled from "styled-components";

export const Container = styled.main`
  color: var(--white);

  h1 {
    font-size: 4.5rem;
    margin-bottom: 4rem;
  }

  div {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }
`;